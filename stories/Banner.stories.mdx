import { Title, Meta, Story, Canvas, ArgsTable, Stories, Source } from '@storybook/addon-docs'
import Banner, { bannerTypes } from '../src/components/Banner/Banner'

<Meta
  title="MDX/Banner"
  component={Banner}
  argTypes={{
    text: '',
    type: {
      options: [
        bannerTypes.primary,
        bannerTypes.secondary,
        bannerTypes.warning,
        bannerTypes.default,
        bannerTypes.cancelled,
      ],
    },
    children: '',
    className: '',
  }}
/>

export const Template = (args) => <Banner {...args} />

<Title />

Testing this

```javascript
bannerTypes = {
  cancelled: 'cancelled',
  default: 'default',
  primary: 'primary',
  secondary: 'secondary',
  warning: 'warning',
}
```

```javascript
<Banner text="Bolded banner text" type={bannerTypes.primary} children="Smaller banner text below bolded">
```

## Variants

<Canvas>
  <Story
    name="Primary"
    args={{
      color: bannerTypes.primary,
      text: 'primary example',
      children: 'Smaller banner',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      type: bannerTypes.secondary,
      text: 'secondary example',
      children: 'Smaller banner',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Cancelled"
    args={{
      type: bannerTypes.cancelled,
      text: 'cancelled example',
      children: 'Smaller banner',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      type: bannerTypes.warning,
      text: 'warning example',
      children: 'Smaller banner',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
      name="Default"
      args={{
        type: bannerTypes.default,
        text: 'default example',
        children: 'Smaller banner',
      }}
    >
      {Template.bind({})}
    </Story>
</Canvas>

Primary: {add description on usage}

Secondary: {add description on usage}

Default: {add description on usage}

Cancelled: {add description on usage}

Warning: {add description on usage}

<ArgsTable />
